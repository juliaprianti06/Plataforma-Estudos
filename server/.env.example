# Local Python: localhost. Docker Compose overrides the host with db.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/estudos_colaborativos
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
ACCESS_TOKEN_EXPIRE_MINUTES=60
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
